# Demo Site

This is a demo Astro project to test the `astro-og-canvas` package.

`cd` into this directory and run `pnpm start` to run the project with a development server or `pnpm build` to build it.
